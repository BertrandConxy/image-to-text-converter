<!DOCTYPE html>
<html>
<head>
  <title>Image-to-text-converter</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" type="text/css" href="../style.css">
</head>
<body>
  <h2 class="card__heading">Image -> Text Converter</h2>
  <div class="card">
    <form action="/upload" method="post" class="form" enctype="multipart/form-data">
      <div class="form__control">
        <label for="amount">Attach Image</label>
        <input type="file" name="image" placeholder="Attach File" required />
      </div>
      <button type="submit" class="btn">Upload</button>
    </form>
  </div>

  <div class="card">
    <% if (error) { %>
      <p>Recognized text:</p>
      <pre><%= error %></pre>
    <% } %>

    <% if (text) { %>
      <p>Recognized text:</p>
      <pre><%= text %></pre>
    <% } %> 
  </div>
</body>
</html>